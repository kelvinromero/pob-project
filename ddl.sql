CREATE TABLE services (NAME VARCHAR(255) NOT NULL, PRICE FLOAT, PRIMARY KEY (NAME))
CREATE TABLE pets (ID  SERIAL NOT NULL, NAME VARCHAR(255), WEIGHT FLOAT, BREED_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE breeds (ID  SERIAL NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE people (DOCUMENT VARCHAR(255) NOT NULL, DTYPE VARCHAR(31), NAME VARCHAR(255), PHONE VARCHAR(255), SERVICEORDERS BYTEA, PRIMARY KEY (DOCUMENT))
CREATE TABLE status (NAME VARCHAR(255) NOT NULL, PRIMARY KEY (NAME))
CREATE TABLE people_pets (Tutor_DOCUMENT VARCHAR(255) NOT NULL, pets_ID INTEGER NOT NULL, PRIMARY KEY (Tutor_DOCUMENT, pets_ID))
ALTER TABLE pets ADD CONSTRAINT FK_pets_BREED_ID FOREIGN KEY (BREED_ID) REFERENCES breeds (ID)
ALTER TABLE people_pets ADD CONSTRAINT FK_people_pets_pets_ID FOREIGN KEY (pets_ID) REFERENCES pets (ID)
ALTER TABLE people_pets ADD CONSTRAINT FK_people_pets_Tutor_DOCUMENT FOREIGN KEY (Tutor_DOCUMENT) REFERENCES people (DOCUMENT)

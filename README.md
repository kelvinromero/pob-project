# Projeto de Persitência de objetos

## Dependências
- Docker + Docker Compose
- make (`sudo apt install make`)

## Como iniciar o ambiente

Você só precisa executar o comando `make up` e o ambiente será iniciado.